✅ Итоговое Техническое Задание (обновлённое)
📌 Проект: Telegram-бот для общения с ИИ по подписке

🧠 Описание проекта
Бот предназначен для приватного общения с ИИ (через OpenRouter), доступ к которому получают только пользователи с активной подпиской. Подписка реализована через вступление в приватный Telegram-канал по временной ссылке (1 месяц). Целевая аудитория — до 10 человек (друзья).

⚙️ Основной функционал
📩 Работа с сообщениями
- Приём текстовых и фото-сообщений от пользователя.
- Отправка текста и изображений в OpenRouter для обработки (модель: Qwen3 235B A22B (free)).
- Получение и отправка ответа пользователю.
- Сохранение контекста диалога (в рамках одной сессии).

🖼 Работа с фото
- Фото, отправленные пользователем, прикрепляются к текущему контексту.
- ИИ обрабатывает изображение вместе с текстом (если есть).
- Технически: изображение отправляется в API OpenRouter (если поддерживается моделью) или преобразуется в base64 + текстовая обвязка.

💡 Команды и кнопки
Команды:
- /start — приветствие и проверка подписки.
- /help — подсказка по функциям.
- /reset — сброс сессии.
- /info — информация о боте, подписке и авторе.

Кнопки:
- Новая сессия
- Помощь
- О боте
- (опционально) Связаться с автором

🔐 Подписка и проверка доступа
- Подписка реализуется через Telegram-канал:
  - Закрытый канал.
  - Пригласительная ссылка со сроком жизни 1 месяц.
- Проверка:
  - При старте (/start) и перед каждым сообщением бот проверяет, состоит ли пользователь в канале.
  - Если нет — сообщение не отправляется, выводится инструкция:
    Доступ к боту платный (150₽/мес).
    Получи ссылку у @sadea12 и вступи в VIP-канал для активации.

🧱 Технологии
- Язык: Python
- Фреймворк: Aiogram
- ИИ API: OpenRouter (Qwen)
- Деплой: Render (free tier)
- Хранение данных: Без БД

💾 Контекст и память
- История диалога хранится в оперативной памяти (dict по user_id).
- Контекст ограничен 1024 символами — при превышении старые сообщения удаляются.
- Один диалог продолжается до вызова команды /reset или перезапуска бота.

🚫 Ограничения
- Нет регистрации или хранения пользователей.
- Нет аналитики, логов, админки.
- Нет масштабирования под большую аудиторию.
- Возможны сбои при превышении лимитов OpenRouter или Render.

Использовать context7 